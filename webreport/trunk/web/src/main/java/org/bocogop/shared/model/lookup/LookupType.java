package org.bocogop.shared.model.lookup;

/**
 * Enums representing different lookup values can implement this interface,
 * which allows us to utilize the enum constants directly for nicer syntax - CPB
 */
public interface LookupType {

	long getId();

}
